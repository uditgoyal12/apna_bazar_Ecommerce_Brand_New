import React, { useEffect, useState } from 'react';
import { Link, useNavigate, useSearchParams, useParams } from 'react-router-dom';
import { api, money, plain } from './api';
import { useStore, useLoad, Icon, Photo, ProductCard, Empty, Loading, ErrorBox, categoryLabel } from './shared';
export function Home() {
  const { catalog } = useStore(), { data, loading, error, reload } = useLoad('/products?limit=8');
  const hero = catalog.maincategory.find(c => c.name === 'Female')?.pic;
  return <><section className="hero"><div className="hero-copy"><div className="eyebrow"><span className="tiny-line"/> A FRESH PERSPECTIVE ON EVERYDAY</div><h1>Good style.<br/>Great days.<br/><em>All yours.</em></h1><p>Pieces you’ll reach for, again and again.<br/>Discover an everyday wardrobe that feels like you.</p><Link className="btn" to="/shop">Explore the collection<Icon name="arrow"/></Link><div className="hero-bottom"><span>CONSIDERED STYLE. EVERYDAY PRICES.</span><span>THE EDIT <i/> 01</span></div></div><div className="hero-visual"><Photo src={hero} alt="The everyday edit — expressive style for every day"/><div className="hero-image-label"><span>THE EVERYDAY EDIT</span><span>Made for your kind of day.</span></div><Link className="hero-circle" to="/shop?maincategory=Female" aria-label="Explore womenswear"><Icon name="arrow" size={28}/></Link></div><span className="hero-side">YOUR STYLE. YOUR STORY.</span></section><div className="benefits"><div><Icon name="truck"/><span>On us, from ₹1,999<small>Complimentary delivery</small></span></div><div><Icon name="bag"/><span>Find your everyday favourites<small>Styles for the whole family</small></span></div><div><Icon name="shield"/><span>A little peace of mind<small>Pay on delivery</small></span></div><div><Icon name="heart"/><span>Here when you need us<small>Personal, thoughtful support</small></span></div></div><section className="section"><div className="section-heading"><div><span className="eyebrow">SOMETHING FOR EVERY SIDE OF YOU</span><h2>Find your kind of style.</h2></div><Link className="underlined" to="/shop">Shop all collections<Icon name="arrow" size={18}/></Link></div><div className="category-grid">{catalog.maincategory.map((c, i) => <Link className="category-card" to={`/shop?maincategory=${encodeURIComponent(c.name)}`} key={c.id}><Photo src={c.pic} alt={`${categoryLabel(c.name)} collection`} loading="lazy"/><span className="category-number">0{i + 1} / THE COLLECTION</span><div><h3>{categoryLabel(c.name)}</h3><span>Explore the edit <Icon name="arrow"/></span></div></Link>)}</div></section><section className="section arrivals"><div className="section-heading"><div><span className="eyebrow">MEET YOUR NEXT FAVOURITES</span><h2>Fresh finds. Lasting favourites.</h2></div><Link className="underlined" to="/shop">View the full edit<Icon name="arrow" size={18}/></Link></div><ErrorBox error={error} retry={reload}/>{loading ? <Loading/> : <div className="product-grid">{data?.items.map(p => <ProductCard product={p} key={p.id}/>)}</div>}</section><section className="editorial"><div className="editorial-image"><Photo src={catalog.maincategory.find(c => c.name === 'Male')?.pic} alt="Personal style, everyday comfort" loading="lazy"/><span>LESS EFFORT. MORE YOU.</span></div><div className="editorial-copy"><span className="eyebrow">THE APNA BAZAR WAY</span><h2>Style that fits<br/>your life.<br/><em>Not the other<br/>way around.</em></h2><p>From slow mornings to spontaneous plans. We bring together familiar brands and fresh discoveries, so getting dressed feels good.</p><Link className="underlined" to="/about">A little about us<Icon name="arrow"/></Link></div></section><section className="brand-section"><span className="eyebrow">FAMILIAR NAMES. NEW FAVOURITES.</span><div className="brand-row">{catalog.brand.slice(0, 6).map(b => <Link to={`/shop?brand=${encodeURIComponent(b.name)}`} key={b.id}>{b.name}</Link>)}</div></section>{catalog.testimonial.length > 0 && <section className="community"><span className="eyebrow">FROM OUR COMMUNITY</span><h2>In good company.</h2><div className="review-grid">{catalog.testimonial.slice(0, 3).map(t => <article key={t.id}><span className="quote-mark">“</span><p>{plain(t.message)}</p><div><Photo src={t.pic} alt=""/><span>{t.name}<small>Apna Bazar community</small></span></div></article>)}</div><Link className="underlined" to="/testimonials">Meet the community<Icon name="arrow" size={17}/></Link></section>}</>;
}
export function Shop() {
  const [params, setParams] = useSearchParams(), { catalog } = useStore();
  const { data, error, loading, reload } = useLoad(`/products?${params.toString()}`);
  const update = (key, value) => { const p = new URLSearchParams(params); value ? p.set(key, value) : p.delete(key); if (key !== 'page') p.delete('page'); setParams(p); };
  return <main className="page"><div className="breadcrumb"><Link to="/">Home</Link> / The collection</div><div className="shop-title"><div><span className="eyebrow">THE EVERYDAY EDIT</span><h1>{params.get('search') ? `Results for “${params.get('search')}”` : params.get('maincategory') ? `${categoryLabel(params.get('maincategory'))}, your way.` : params.has('sale') ? 'Good style. Even better prices.' : 'Your next favourite is here.'}</h1><p>A considered collection for the way you live.</p></div><span className="result-count">{data?.total || 0} pieces to discover</span></div><div className="collection-tabs"><button className={!params.has('maincategory') ? 'active' : ''} onClick={() => update('maincategory', '')}>All pieces</button>{catalog.maincategory.map(c => <button className={params.get('maincategory') === c.name ? 'active' : ''} key={c.id} onClick={() => update('maincategory', c.name)}>{categoryLabel(c.name)}</button>)}</div><div className="shop-layout"><aside className="filters"><div className="filter-title">Refine your edit<button onClick={() => setParams({})}>Reset</button></div><form className="filter-search" onSubmit={e => { e.preventDefault(); update('search', new FormData(e.currentTarget).get('search')); }}><input key={params.get('search')} name="search" aria-label="Search products" defaultValue={params.get('search') || ''} placeholder="Search pieces…"/><button aria-label="Apply search"><Icon name="search" size={18}/></button></form>{[['subcategory','Product type'],['brand','Brand']].map(([key, label]) => <div className="filter-group" key={key}><h3>{label}</h3>{catalog[key].map(c => <label className="check-label" key={c.id}><input type="checkbox" checked={params.get(key) === c.name} onChange={() => update(key, params.get(key) === c.name ? '' : c.name)}/>{c.name}</label>)}</div>)}<div className="filter-group"><h3>A few preferences</h3><label className="check-label"><input type="checkbox" checked={params.has('inStock')} onChange={e => update('inStock', e.target.checked ? 'true' : '')}/>In stock only</label><label className="check-label"><input type="checkbox" checked={params.has('sale')} onChange={e => update('sale', e.target.checked ? 'true' : '')}/>On sale</label></div><div className="filter-note"><Icon name="truck" size={26}/><p>A little extra, on us.</p><small>Free delivery on orders<br/>of ₹1,999 or more.</small></div></aside><div><div className="sort-bar"><span>{loading ? 'Finding your favourites…' : `${data?.total || 0} results`}</span><label>Sort by <select aria-label="Sort products" value={params.get('sort') || 'newest'} onChange={e => update('sort', e.target.value)}><option value="newest">The latest edit</option><option value="price-low">Price: low to high</option><option value="price-high">Price: high to low</option><option value="name">Name: A to Z</option></select></label></div><ErrorBox error={error} retry={reload}/>{loading ? <Loading/> : data?.items.length ? <div className="product-grid shop-products">{data.items.map(p => <ProductCard product={p} key={p.id}/>)}</div> : !error && <Empty title="Nothing here just yet." text="Try a different search or clear a filter to find your next favourite."/>}{data?.pages > 1 && <div className="pagination"><button disabled={data.page === 1} onClick={() => update('page', String(data.page - 1))}>Previous</button><span>Page {data.page} of {data.pages}</span><button disabled={data.page === data.pages} onClick={() => update('page', String(data.page + 1))}>Next <Icon name="arrow" size={16}/></button></div>}</div></div></main>;
}
export function Product() {
  const { id } = useParams(), { data: p, error, loading, reload } = useLoad(`/products/${id}`), { user, setCart, wishlist, refresh, notify } = useStore(), navigate = useNavigate(), [image, setImage] = useState(0), [qty, setQty] = useState(1), [busy, setBusy] = useState(false);
  useEffect(() => { setImage(0); setQty(1); }, [id]);
  if (loading) return <Loading/>; if (error) return <main className="page"><ErrorBox error={error} retry={reload}/></main>;
  const saved = wishlist.some(w => w.id === id);
  return <main className="page"><div className="breadcrumb"><Link to="/">Home</Link> / <Link to="/shop">Collection</Link> / {p.name}</div><div className="product-detail"><div><div className="detail-image"><Photo src={p.pic[image]} alt={p.name}/></div><div className="thumbnails">{p.pic.map((src, i) => <button key={src + i} className={i === image ? 'selected' : ''} onClick={() => setImage(i)} aria-label={`Product image ${i + 1}`}><Photo src={src} alt={`${p.name} view ${i + 1}`}/></button>)}</div></div><div className="detail-copy"><span className="eyebrow">{p.brand} / {categoryLabel(p.maincategory)}</span><h1>{p.name}</h1><div className="detail-price">{money(p.finalPrice)}{p.basePrice > p.finalPrice && <><del>{money(p.basePrice)}</del><span>Save {p.discount}%</span></>}</div><small className="muted">Inclusive of all taxes</small><p className="detail-description">{plain(p.description) === 'Test Product' ? `An everyday ${p.subcategory.toLowerCase()} from ${p.brand}. Pair it your way and make it part of your everyday edit.` : plain(p.description)}</p><div className="variant-row"><span>Colour: <strong>{p.color}</strong></span></div><div className="variant-row"><span>Available size</span><span className="size-chip">{p.size}</span></div><p className={p.stockQuantity ? 'stock-status' : 'muted'}>{p.stockQuantity ? `${p.stockQuantity < 6 ? `Only ${p.stockQuantity} left` : 'In stock'} · Ready for your wardrobe` : 'Currently out of stock'}</p><div className="add-row"><div className="quantity"><button disabled={qty <= 1 || busy} aria-label="Decrease quantity" onClick={() => setQty(qty - 1)}>−</button><span>{qty}</span><button disabled={qty >= Math.min(p.stockQuantity, 20) || busy} aria-label="Increase quantity" onClick={() => setQty(qty + 1)}>+</button></div><button className="btn" disabled={!p.stockQuantity || busy} onClick={async () => { if (!user) return navigate('/login', { state: { from: `/product/${id}` } }); setBusy(true); try { setCart(await api('/cart', { method: 'POST', body: { product: id, qty, size: p.size } })); notify('A good choice. Added to your bag.'); } catch (e) { notify(e.message); } finally { setBusy(false); } }}>{busy ? 'Adding…' : 'Add to bag'}<Icon name="bag"/></button></div><button className="btn outline full" disabled={busy} onClick={async () => { if (!user) return navigate('/login'); setBusy(true); try { await api(`/wishlist/${id}`, { method: saved ? 'DELETE' : 'POST' }); await refresh(); } catch (e) { notify(e.message); } finally { setBusy(false); } }}><Icon name="heart"/>{saved ? 'Saved to your wishlist' : 'Save for a little later'}</button><div className="detail-perks"><p><Icon name="truck"/>Complimentary delivery on orders ₹1,999+</p><p><Icon name="shield"/>Cash on delivery available</p></div><details open><summary>The details</summary><p>{p.brand} · {p.subcategory} · {p.color} · Size {p.size}</p></details><details><summary>Delivery & support</summary><p>Delivery charges are calculated in your bag. For delivery questions or help with an item, <Link to="/contactus">contact our team</Link>.</p></details></div></div></main>;
}
export function Contact() { const { config, notify } = useStore(), [error, setError] = useState(''), [busy, setBusy] = useState(false); return <main className="page"><div className="page-heading"><span className="eyebrow">REAL PEOPLE. A LITTLE HELP.</span><h1>Let’s talk.</h1><p>Questions, feedback or a simple hello. We’re all ears.</p></div><div className="contact-layout"><div className="contact-info"><h2>Here for you.</h2><p>Need a hand with your order or finding the right piece? Send us a message.</p><h4>Email us</h4><a href={`mailto:${config.email}`}>{config.email}</a><h4>Give us a call</h4><a href={`tel:${config.phone}`}>{config.phone}</a><h4>Find us</h4><p>{config.address}</p></div><form className="form-panel" onSubmit={async e => { e.preventDefault(); const form = e.currentTarget; setBusy(true); setError(''); try { const r = await api('/contactus', { method: 'POST', body: Object.fromEntries(new FormData(form)) }); notify(r.message); form.reset(); } catch (e) { setError(e.message); } finally { setBusy(false); } }}><label>Your name<input name="name" required/></label><label>Email address<input name="email" type="email" required/></label><label>What’s on your mind?<input name="subject" required/></label><label>Your message<textarea name="message" rows={5} maxLength={3000} required/></label><ErrorBox error={error}/><button className="btn" disabled={busy}>{busy ? 'Sending…' : 'Send your message'}<Icon name="arrow"/></button></form></div></main>; }
export function About() { const { catalog } = useStore(); return <main><section className="about-hero"><span className="eyebrow">HELLO, WE’RE APNA BAZAR.</span><h1>Everyday pieces.<br/><em>Extraordinary you.</em></h1><p>We believe good style should feel natural.<br/>A little considered. A little unexpected. Always you.</p></section><section className="editorial"><div className="editorial-image"><Photo src={catalog.maincategory.find(c => c.name === 'Male')?.pic} alt="Everyday style inspiration"/></div><div className="editorial-copy"><span className="eyebrow">A WARDROBE FOR REAL LIFE</span><h2>Find something<br/>that feels like you.</h2><p>Apna Bazar brings together clothing from familiar brands for women, men and kids. From the jeans you live in to a fresh layer for the weekend, our collection is here to make everyday dressing simpler.</p><p>Explore at your own pace, save your favourites and build your own everyday edit.</p><Link className="btn" to="/shop">Find your favourites<Icon name="arrow"/></Link></div></section><section className="section values-grid">{[['01','Your style, your way','Explore collections for different days, moods and moments.'],['02','The details matter','Clear prices, available sizes and stock information on every piece.'],['03','People come first','A real team to help with questions about your order.']].map(([n,t,d]) => <article key={n}><span className="eyebrow">{n} / OUR APPROACH</span><h2>{t}</h2><p>{d}</p></article>)}</section></main>; }
export function Testimonials() { const { catalog, user, notify } = useStore(), [busy, setBusy] = useState(false); return <main className="page"><div className="page-heading"><span className="eyebrow">STYLE IS BETTER SHARED</span><h1>Our kind of people.</h1><p>Stories from the Apna Bazar community.</p></div><div className="review-grid">{catalog.testimonial.map(t => <article key={t.id}><span className="quote-mark">“</span><p>{plain(t.message)}</p><div><Photo src={t.pic} alt=""/><strong>{t.name}</strong></div></article>)}</div><div className="review-form"><h2>A little about your experience?</h2>{user ? <form onSubmit={async e => { e.preventDefault(); const f = e.currentTarget; setBusy(true); try { const r = await api('/testimonials', { method: 'POST', body: { message: new FormData(f).get('message') } }); notify(r.message); f.reset(); } catch (e) { notify(e.message); } finally { setBusy(false); } }}><label>Your review<textarea required minLength={10} maxLength={1000} name="message" rows={4}/></label><button className="btn" disabled={busy}>Share your experience<Icon name="arrow"/></button></form> : <Link className="btn" to="/login">Sign in to share your story</Link>}</div></main>; }
